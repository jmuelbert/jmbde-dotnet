extraction:
  csharp:
    index:
      dotnet:
        version: 5.0.103
      solution: jmbde.sln
      msbuild:
        configuration: release   
        platform: x64
      build_command:
        - dotnet build -p:Configuration=Release