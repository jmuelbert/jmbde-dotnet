@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<jmbde.Models.Company>
@{
	ViewBag.Title = "Company";
}

<h2>
    Company's
</h2>

<p>
	<a asp-action="Create">Create a New Company</a>
</p>

<table id="company" class="table table-condensed table-hover table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name2)
            </th>
            <th>
                <p>Action</p>
            </th>
        </tr>
    </thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name2)
            </th>
            <th>
                <p>Action</p>
            </th>
        </tr>
    <tfoot>
    </tfoot>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modeItem => item.Name2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td> 
        </tr>
    }
</table>

@section scripts {
    <script type="text/javascript">
        $(document).ready(function() {
            $('#employee').DataTable();
        } );
    </script>
}