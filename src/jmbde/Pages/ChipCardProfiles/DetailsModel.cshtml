@page "{id:int}"
@model JMuelbert.BDE.Pages.ChipCardProfiles.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ChipCardProfile</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ChipCardProfile.Number)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ChipCardProfile.Number)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ChipCardProfile.LastUpdate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ChipCardProfile.LastUpdate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ChipCardProfile.Employee)
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                <tr>
                    <th>FirstName</th>
                    <th>LastName</th>
                </tr>
                @foreach (var item in Model.ChipCardProfile.Employee)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.FirstName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LastName)
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ChipCardProfile.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
