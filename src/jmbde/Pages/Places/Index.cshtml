@page
@model jmbde.Pages.Places.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>

<form>
    <p>
        Name: <input type="text" name="SearchString">
        <input type="submit" value="Filter" />
    </p>
</form>

<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Place[0].Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Place[0].Room)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Place[0].Desk)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Place[0].LastUpdate)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Place) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PlaceId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PlaceId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PlaceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
